pre-commit:
  commands:
    gitleaks:
      run: gitleaks protect --staged
    checkov:
      glob: "*.tf"
      run: checkov -d . --framework terraform --compact --quiet
    tflint:
      glob: "*.tf"
      run: tflint --recursive
